import { FastForward } from "lucide-react";
export default function Start() {
  return (
    <div className="go w-full h-full flex items-center justify-center bg-green-900 flex-col rounded-lg">
      GO
      <FastForward size={25} />
    </div>
  );
}
